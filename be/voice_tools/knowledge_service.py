from __future__ import annotations

import json
from pathlib import Path
from typing import Optional


def search_pitch_deck(company_name: str) -> dict:
    """
    Placeholder - knowledge base is already in the dashboard.
    This can be removed or kept for future use.
    """
    return {
        "found": False,
        "message": "Knowledge base moved to dashboard"
    }


def get_relevant_deck(claim: str) -> list:
    """Placeholder - not used."""
    return []
